<?xml version="1.0" encoding="utf-8"?>

<!--

    Changes were made to this file for inclusion in the wX program.

    joshua.tee@gmail.com

-->

<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:id="@+id/record_overlay_buttons"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/record_overlay_cancel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/gray_dark"
            android:clickable="true"
            android:contentDescription="@string/clear"
            android:focusable="true" />

        <ImageButton
            android:id="@+id/record_overlay_distance_tool"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/gray_dark"
            android:clickable="true"
            android:contentDescription="@string/clear"
            android:focusable="true" />

        <ImageButton
            android:id="@+id/record_overlay_drawtool"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/gray_dark"
            android:contentDescription="@string/clear" />
    </LinearLayout>
</merge>
