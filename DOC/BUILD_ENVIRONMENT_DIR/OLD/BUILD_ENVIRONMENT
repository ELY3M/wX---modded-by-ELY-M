

MISC Specs on build environment

UPDATE 12/11/2015:
Linux Mint 17.3
/home/josh/jdk1.8.0_60
SDK Platform Android 6.0 revision 2
SDK Tools 24.4.1
SDK Platform-tools 23.0.1
Android Support Library 23.1.1
API 23 
Studio 1.5.1


UPDATE 9/28/2015:
/home/josh/jdk1.8.0_60
SDK Tools 24.3.4
SDK Platform-tools 23.0.1
Android Support Library 23.0.1
API 23
Studio 1.4RC3


Migrate from existing environment.
-------------------------------------------
to existing VM add 40GB VDI disk called “datavg.vdi”

fdisk -l shows as sdc

pvcreate /dev/sdc
vgcreate datavg /dev/sdc
lvcreate -l 100%FREE -n lvol1 datavg 
mkdir /data
mke2fs -j /dev/datavg/lvol1
tune2fs -c 0 /dev/datavg/lvol1
tune2fs -i 0 /dev/datavg/lvol1
mount /dev/datavg/lvol1 /data
df

cd /home
rsync -a jtgreen /data/
cd /data
mv jtgreen josh
cd josh
rm -rf .config
cd / ; umount /data
lvchange -a n  /dev/datavg/lvol1
vgexport datavg

---- move disk to new VM built using specs below

vgimport datavg
vi /etc/fstab

mount -a
cd /home
ls -l 
shutdown -r now

---------------------------------

Testing in process with new build environment ( 2015 Sep )

Linux Mint 17.2 ( desktop amd64 )
 install updates during install and install 3rd party software
  3GB memory and 16GB root disk with LVM
  change drag and drop to bidirectional
  install guest additions 
        
Android Studio 1.2.1.1
NDK r10e

Additional software:
apt-get -y install lib32stdc++6 lib32z1
#apt-get -y  install default-jdk


Start script for studio
josh@josh-VirtualBox:~$ cat run
#!/bin/bash
#IBUS_ENABLE_SYNC_MODE=1 ibus-daemon -xrd
#export JAVA_HOME=/usr/lib/jvm/default-java
export JAVA_HOME=/home/josh/jdk1.8.0_60
android-studio/bin/studio.sh


----------------------
dev environment has android studio 1.3  and here is the build.gradle in use

apply plugin: 'com.android.application'

android {
    compileSdkVersion 'Google Inc.:Google APIs:22'
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "joshuatee.wx"
        minSdkVersion 17
        targetSdkVersion 22

        ndk {
            moduleName "radial"
            //  ldLibs "log"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    compile 'com.android.support:appcompat-v7:22.1.1'
    compile 'com.squareup.okhttp:okhttp:2.4.0'
    compile files('libs/apache-bzip.jar')
    //compile files('libs/commons-net-3.3.jar')
}



-------------------------
gradle perf enhancement
------------------------

app/gradle.properties
add
org.gradle.jvmargs=-Xmx2048m
